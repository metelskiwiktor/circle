project:
  env:
    google:
      enabled: false
    web:
      start-browser: false
      frontend-url:
spring:
  h2:
    console:
      enabled: false
  datasource:
    url: jdbc:h2:mem:circle-it
    username: h2admin
    password: admin
  jpa:
    hibernate:
      ddl-auto: create-drop
management:
  endpoints:
    enabled-by-default: false
server:
  port: 8080
